class SoundAnim {
    //Trying to make something for general, practical use
    //Ideally should create "choreographed" sprite so that a sprite can make sounds on specific frames

    constructor(scene, key, config, soundIndex){
        
    }

    //soundIndex should look something like the following
    /*
    soundIndex = {
        0: {
            sound: 'sound name'
        },
        7: {
            sound: 'sound name'
        }
    }
    Where 0 or 7 is the frame the sound should occur on
    */
}